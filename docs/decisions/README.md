# Architecture Decision Records

凡是会影响 benchmark 公平性、可复现性、目录兼容性或发布口径的决定，都新增一个 ADR；不要回写旧 ADR 来掩盖历史。

命名格式：`NNNN-short-title.md`。状态使用 `Proposed`、`Accepted`、`Superseded` 或 `Rejected`；被替代时互相链接。

最小内容：背景、决定、理由、后果和迁移影响。
